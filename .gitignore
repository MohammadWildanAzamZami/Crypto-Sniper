# Rust
/target

# Node
node_modules/
**/node_modules/

# Secrets / environment
.env
*.env
.env.*
!.env.example
# Persisted Settings-panel store (holds the same API keys as .env)
.settings.json
**/.settings.json
# Persisted 10x Radar runtime state (latest scan + alerted mints)
.radar-state.json
**/.radar-state.json
# Pro Radar self-learning memory (tracked picks + graded outcomes + tuned thresholds)
.radar-memory.json
**/.radar-memory.json
# Sniper Engine smart-wallet watchlist (Modul B — learned wallet track records)
.watchlist-state.json
**/.watchlist-state.json
# Sniper Engine live monitor signals (Modul C) — v2 (tajam) + awal (headcount)
.sniper-state.json
**/.sniper-state.json
.sniper-awal-state.json
**/.sniper-awal-state.json
# Sniper Engine v2 runtime parameter overrides (Settings UI)
.sniper-params.json
**/.sniper-params.json
# Sniper Engine PnL track record (Modul C — graded signal outcomes)
.sniper-track.json
**/.sniper-track.json
# Sniper Engine transaction log (Modul C — every monitored-wallet swap, rolling)
.sniper-txs.json
**/.sniper-txs.json
# Token identity cache (mint → symbol/logo) for tx-log & sniper rows
.token-meta.json
**/.token-meta.json
# Wallet Intelligence v2 (state pipeline + override parameter WI dari Settings UI)
.wallet-intel.json
**/.wallet-intel.json
.wallet-intel-params.json
**/.wallet-intel-params.json
# Influencer watchlist (Modul B2)
.influencers-state.json
**/.influencers-state.json

# EVM / Robinhood Chain sniper runtime state
**/.evm-sniper-state.json
**/.evm-watchlist-state.json

# Helius webhook registration (id + auth token) for real-time sniper
**/.helius-webhook.json

# Build output
dist/
build/
web/frontend/dist/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
